import 'package:flutter/material.dart';

class SpotPricePage extends StatelessWidget {
  const SpotPricePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Spot Price Page"),),);
  }
}
